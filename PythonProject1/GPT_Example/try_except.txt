В Python конструкція try-except використовується для обробки винятків (помилок), які можуть виникнути під час виконання коду. Це дозволяє програмі продовжити роботу, навіть якщо сталася помилка, замість того щоб аварійно завершитися.

📌 Базова структура try-except:
try:
    # Тут код, який може викликати помилку
except:
    # Код, який виконається, якщо виникла помилка

Як це працює:
try — у цьому блоці виконується код, який може викликати помилку.

except — якщо сталася помилка, виконується код у цьому блоці.

Якщо помилки немає — except пропускається.

🧑‍💻 Приклад 1: Обробка простої помилки

try:
    number = int(input("Введіть число: "))
    print(f"Ваше число: {number}")
except:
    print("❌ Ви ввели не число!")
✅ Як працює цей код:

Якщо ввести, наприклад, "42" — програма продовжить роботу.

Якщо ввести "hello" — відловиться помилка ValueError, і програма виведе "❌ Ви ввели не число!".

🔍 Приклад 2: Обробка конкретної помилки
Можна обробити конкретний тип помилки, щоб діяти точніше.


try:
    x = 10 / 0  # Ділення на нуль
except ZeroDivisionError:
    print("❌ Ділення на нуль неможливе!")
Якщо сталася помилка ZeroDivisionError, програма не зламається, а обробить її.

📌 Корисні типи винятків у Python:
ValueError — неправильний тип даних (наприклад, введено текст замість числа).

ZeroDivisionError — ділення на нуль.

IndexError — звернення до неіснуючого індексу у списку.

KeyError — відсутність ключа у словнику.

FileNotFoundError — файл не знайдено.

TypeError — неправильна операція для типу (наприклад, додавання рядка до числа).

🧰 Приклад 3: Відловлення кількох помилок
Можна обробити декілька типів помилок одразу.

try:
    x = int(input("Введіть число: "))
    y = 10 / x
    print(f"Результат: {y}")
except ValueError:
    print("❌ Потрібно вводити число!")
except ZeroDivisionError:
    print("❌ Не можна ділити на нуль!")
📢 Приклад 4: else та finally
else — виконується, якщо помилки не було.

finally — виконується завжди, навіть якщо була помилка.


try:
    num = int(input("Введіть число: "))
except ValueError:
    print("❌ Це не число!")
else:
    print("✅ Успіх! Ви ввели:", num)
finally:
    print("🔚 Програма завершила роботу.")

🔍 Порада: Як дізнатися, яка саме помилка сталася
Щоб побачити текст помилки, використовуйте except Exception as e:


try:
    print(10 / 0)
except Exception as e:
    print(f"❌ Сталася помилка: {e}")
👉 Виведе: ❌ Сталася помилка: division by zero.

📌 Коли використовувати try-except:
Для перевірки введення користувача (як у вашій програмі).

Робота з файлами (якщо файл відсутній).

Виконання запитів до мережі (можуть бути помилки підключення).

Будь-які місця, де помилка може виникнути, але програма повинна продовжити роботу.